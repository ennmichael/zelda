# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# TODO: Separate development dependencies

gem 'gosu'
gem 'minitest'
gem 'rake'
gem 'solargraph'
